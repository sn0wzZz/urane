---
const links = [
  {
    name: 'За фирмата',
    links: [
      { name: 'За нас', href: '/about' },
      { name: 'Процеси', href: '/' },
      { name: 'Контакти', href: '/' },
      { name: 'Карта на сайта', href: '/' },
    ],
  },
  {
    name: 'Услуги',
    links: [
      { name: 'Обучения', href: '/' },
      { name: 'Подготовка на документи', href: '/' },
      { name: 'Помощен център', href: '/' },
      { name: 'Ресурси', href: '/' },
    ],
  },
]

const today = new Date()
---

<footer class='container mx-auto flex flex-col gap-16 py-8'>
  <div class='grid grid-cols-2 max-w-md'>
    {
      links.map((link) => (
        <div class='flex flex-col gap-4'>
          <p class='text-muted-foreground'>{link.name}</p>
          <ul class='gap-4 flex flex-col'>
            {link.links.map((link) => (
              <li>
                <a href={link.href} class='hover:underline'>
                  {link.name}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))
    }
  </div>
  <div class='flex flex-col md:flex-row md:justify-between gap-4'>
    <p class='text-muted-foreground'>
      &copy; {today.getFullYear()} - УРАНЕ, Всички права запазени
    </p>
    <ul
      class='flex gap-8 text-xs md:text-base [&_li]:underline [&_li]:underline-offset-5 [&_a]:hover:text-muted-foreground'
    >
      <li><a href='/'>Общи условия</a></li>
      <li><a href='/'>Поверителност</a></li>
      <li><a href='/'>Лични данни</a></li>
    </ul>
  </div>
</footer>
